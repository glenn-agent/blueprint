# Copy to ~/.openclaw/.env on the OpenClaw host. Fill only the values you use.
# NEVER commit real secrets.

# NVIDIA inference gateway API key
# Endpoint: https://inference-api.nvidia.com
NV_API_KEY=

# GitHub token for gh CLI
GH_TOKEN=
