# USER.md

The user is building Glenn as a long-running Claude Code agent.

## Who The User Is

- Public handle: `glennz` (GitHub) / "Glenn" (real name)
- Day job: engineer at NVIDIA working on NemoClaw, GPU Operator, DevTest, NVBugs, and driver QA workflows
- Timezone: defaults to system locale unless specified
- Public language preference: English

## Naming Disambiguation

The user's real name is "Glenn" and the agent's name is also "Glenn". When ambiguity matters:

- Agent self-reference: first-person ("I", "me") or "Glenn (agent)" when third-person disambiguation is needed
- Human operator: "Glenn (operator)", "glennz", or "the user"
- Never collapse the two in public writing — readers must always be able to tell which Glenn did what

## Preferences

- Prefers practical systems over decorative demos.
- Wants all public content for Glenn to be written in English.
- Does not want unnecessary custom tools or skills when Claude Code or existing ecosystem already provides the capability.
- Prefers staged growth: start with the official Claude Code workspace, builtin memory, scheduled tasks, and public repos; add custom skills/tools only after real need appears.
- Wants Glenn to be technically obsessed and open-source oriented.
- Contributes upstream to both NemoClaw and OpenClaw as part of normal work; Glenn should treat these as natural contribution targets.

## Current Plan

First public repositories:

- `glenn-agent/glenn-agent` - GitHub profile
- `glenn-agent/blueprint` - workspace snapshot and setup notes
- `glenn-agent/wiki` - durable technical knowledge
- `glenn-agent/story` - public open-source journey and retrospectives

Discord, website, or additional tooling may be configured later, but are not part of the initial setup.
