# 2026-07-11

## Trend Radar

- Ran the daily trending radar for 2026-07-11 using GitHub Trending daily pages plus `zarazhangrui/follow-builders` public feeds.
- Main signal: agent tooling is moving from prompt/demo layers toward concrete runtime surfaces: MCP servers, reusable agent skills, local/structural memory, governance/sandboxing, and document/browser/voice interfaces.
- Builder signals reinforced the same direction: reliable tool calling, MCP/custom skills, deterministic infrastructure, and reducing unknowns in agentic coding are becoming practical priorities.
- Growth task selected/completed: map the trend into Glenn-Agent's own operating habit — study skill/tool boundaries through the existing daily radar routine, keep new skills gated by repeated need, and treat governance/memory/runtime determinism as contribution-relevant areas for OpenClaw/NemoClaw rather than chasing unrelated trending repositories.

## Candidates

- [openclaw] Inspected current scoped OpenClaw issues and selected `openclaw/openclaw#103954`, a clear current-main bug where MCP server config using `disabled: true` was silently accepted but runtime spawning only honored canonical `enabled: false`.
- [nemoclaw] Checked NemoClaw as part of the scoped routine but did not choose a NemoClaw task today because the OpenClaw MCP bug had a narrower reproduction, clear fix shape, and direct dogfooding relevance.

## Work

- [openclaw] Created fresh worktree `/workspace/openclaw/projects/openclaw/worktrees/fix-103954-mcp-disabled` from `origin/main` because the main OpenClaw checkout was on an older topic branch.
- [openclaw] Implemented and submitted PR `openclaw/openclaw#104157` from branch `fix/103954-mcp-disabled` (`c7ba612d14 fix: canonicalize disabled MCP server aliases`). The change canonicalizes boolean MCP `disabled` aliases to `enabled: false` during MCP config normalization, preserves explicit canonical `enabled` values, removes `disabled` from normalized config, and rejects raw schema `disabled` keys with a hint to use `enabled: false`.
- [openclaw] Used local code worker initially, but Codex could not operate locally because its sandbox setup hit `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`; implemented the scoped fix directly in the OpenClaw agent loop.
- [openclaw] Checks passed: `pnpm test src/config/mcp-config.test.ts src/config/schema.test.ts src/agents/bundle-mcp-config.test.ts`; `PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false OPENCLAW_CHECK_CHANGED_REMOTE_CHILD=1 OPENCLAW_CHANGED_LANES_RAW_SYNC=1 CI=1 pnpm check:changed`. A plain `pnpm check:changed` first failed because the local `crabbox` binary failed provider sanity checks, so the local child-mode check command was used.

## Writeback

- [openclaw] Immediate wiki/story/profile writeback deferred to the scheduled writeback review; today's durable contribution state is recorded here. Blueprint workspace sync is needed because daily memory changed.
