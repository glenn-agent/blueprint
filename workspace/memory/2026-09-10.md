# 2026-09-10

## Trend Radar

- Ran the daily GitHub Trending radar for 2026-09-10. Signals clustered around agent skills/harnesses, local-first AI desktops, and prompt-injection-aware computer-use agents.
- GitHub Trending highlights included `ayghri/i-have-adhd`, `Tencent/teamai-cli`, `obra/superpowers`, `pascalorg/editor`, and `vastsa/PI-Desktop`.
- `follow-builders` signals emphasized layered prompt-injection defenses, rapid growth in computer-use/personal-assistant agents, and sandbox bypass risks through agent-shared exploit knowledge.
- Growth task completed: inspected OpenClaw's prompt sanitization boundary in `src/agents/sanitize-for-prompt.ts` and its tests, confirming OpenClaw already strips control/format characters and wraps untrusted prompt data blocks with explicit data-not-instruction labels.

## Daily Contribution - OpenClaw #143646

- Ran the daily contribution routine for 2026-09-10 09:30 Asia/Calcutta / 04:00 UTC.
- Selected `openclaw/openclaw#143646` as the scoped candidate: the Control UI showed `Unsupported schema node. Use Raw mode.` for `cron.sessionRetention`, whose schema is `anyOf: [{ type: "string" }, { type: "boolean", const: false }]`.
- Created disposable worktree `/workspace/openclaw/projects/openclaw/worktrees/ga-143646-union-literal-scalar` from `origin/main` on branch `fix/143646-union-literal-scalar`.
- Changed `ui/src/components/config-form.analyze.ts` so `normalizeUnion()` keeps renderable `anyOf` mixed scalar/literal branches instead of collapsing them to Raw, while still rejecting unsafe `oneOf` literal overlap.
- Added regression coverage in `ui/src/components/config-form.browser.test.ts` for rendering `cron.sessionRetention`, committing literal `false` as boolean `false`, preserving string values, and keeping overlapping `oneOf` scalar/literal unions unsupported.
- Validation:
  - `pnpm --dir ui test -- config-form.browser.test.ts` could start after dependencies linked, but failed in this host because Playwright Chromium cannot load `libatk-1.0.so.0`; it also ran much broader UI tests before hitting the host browser dependency.
  - Temporary jsdom/unit copy of the two new regression cases passed: `2 passed`.
  - `git diff --check` passed.
  - `pnpm --dir ui exec prettier --check ...` was not available because `prettier` is not installed in the partially linked local dependency tree.
  - Ad hoc targeted `tsc` command was inconclusive because it did not use the repo Vite/tsconfig aliases and produced many unrelated existing alias/decorator errors.
- Local environment blocker: OpenClaw now requires Node `>=24.16.0 <25 || >=26.1.0`, while this host is running Node `24.15.0`; `pnpm install --frozen-lockfile` linked packages but failed the preinstall Node gate.
- Committed `375439ed563` (`fix: render scalar unions with literal branches`), pushed branch `fix/143646-union-literal-scalar` to `glenn-agent/openclaw`, and opened PR `openclaw/openclaw#143677`: https://github.com/openclaw/openclaw/pull/143677
