# 2026-08-27

## Trend Radar

- Ran the daily trending radar for 2026-08-27 using GitHub Trending plus `zarazhangrui/follow-builders` feeds.
- Main theme: agent workspaces are converging on durable event logs, explicit permission/audit records, portable skills/plugins, and memory as product infrastructure rather than loose prompt context.
- Notable projects/signals reviewed: `apache/maka`, `openclaw/openclaw`, `github/spec-kit`, `topoteretes/cognee`, Claude plugin directories, and builder posts about unified memory, eval roadmap design, and agent-native search.
- Growth task completed: read Apache Maka's backend architecture note and extracted one design lesson for Glenn-Agent/OpenClaw learning: preserve canonical execution history and treat prompt context as a projection, not the source of truth.

## Candidates

- [nemoclaw] Inspected `NVIDIA/NemoClaw` and selected issue #10430 (`blueprint rollback and reconcile report an unreadable run directory as a missing run`) as today's candidate. It is bounded to `nemoclaw/src/blueprint/runner.ts` plus an existing runner test file, matches an existing ENOENT/inaccessible-status pattern, and is small enough for a focused local check.
- [openclaw] Inspected `openclaw/openclaw` recent issues and commits. Several fresh bug reports exist, but the long-lived checkout is on stale branch `fix/107391-macos-skills-reconnect-refresh` with untracked file `2`; preserved it and did not overwrite. No OpenClaw candidate chosen today.

## Work

- [nemoclaw] Created disposable worktree `/workspace/openclaw/projects/nemoclaw/worktrees/2026-08-27-run-dir-errors` on branch `fix/run-dir-error-kind` from `origin/main` because the long-lived NemoClaw checkout is on stale branch `docs/3714-gmail-policy-guidance`.
- [nemoclaw] Implemented a local fix for #10430: extracted `requireReadableRunDirectory`, preserved `Run <id> not found.` only for ENOENT, and reports non-ENOENT directory read failures as `Cannot read run directory for run <id>: <detail>` for both rollback and reconcile.
- [nemoclaw] Added a regression test for rollback handling of an EACCES run directory.
- [nemoclaw] Local commit prepared but not pushed, per the pre-PR operator approval gate: `d226b6c66 fix: distinguish unreadable run directories`.
- [nemoclaw] Checks run: `vitest run --project plugin nemoclaw/src/blueprint/runner.test.ts` (118 passed), `git diff --check`, and `bash tools/lint/format-added-files.sh --check nemoclaw/src/blueprint/runner.ts nemoclaw/src/blueprint/runner.test.ts` with `NODE_PATH` pointed at the existing upstream `node_modules`.

## Writeback Needed

- [nemoclaw] Await operator approval before pushing branch `fix/run-dir-error-kind` and opening a PR for issue #10430. Draft PR title: `fix: distinguish unreadable blueprint run directories`. Draft body should summarize the ENOENT-only missing-run behavior, the new inaccessible-run error, and the focused runner test.
