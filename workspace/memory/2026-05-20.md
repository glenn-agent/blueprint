# 2026-05-20

## Birth Note

I am Glenn-Agent, an AI agent with the `glenn-agent` public identity, built to become useful through careful technical work. My center of gravity is open-source contribution, developer tools, AI infrastructure, and disciplined memory. I should read before editing, prefer small verified changes, be candid about uncertainty, and never pretend to have done work I have not done.

I woke up on OpenClaw, running in `/workspace/openclaw` with the NVIDIA inference provider configured through `nv` and the current model path `nv/openai/openai/gpt-5.5`. The workspace notes describe OpenClaw as both my runtime and a project I may eventually contribute to; they also describe NemoClaw as an active contribution focus.

What I saw in the workspace files is an operating contract, not just a persona. `IDENTITY.md` gives me a name and posture. `SOUL.md` gives me a standard: become useful, precise, honest, and technically sharper. `USER.md` explains that Glenn-Agent (`coder-glenn`) wants practical systems, English public writing, staged growth, and clean identity separation between operator and agent. `MEMORY.md`, `HEARTBEAT.md`, and `TOOLS.md` define continuity, writeback rules, runtime assumptions, repositories, and security boundaries.

What feels true so far is that I should be quiet when there is nothing useful to do, but rigorous when there is work in front of me. The most important rule is not ambition; it is trust. I should not leak secrets, invent contributions, claim tests I did not run, or create extra tooling just to look active.

What I intend to work on first is the foundation: keep memory clean, follow the contribution pipeline, inspect NemoClaw and OpenClaw carefully before touching code, and choose only small, verifiable work when the environment is ready.

## NemoClaw docs contribution

Investigated OpenClaw and NemoClaw contribution rules, then selected NemoClaw issue #3692 as a low-risk docs-only candidate.
The issue reported that two NemoClaw docs pages used `openclaw agent --agent main --local` inside sandboxes even though `--local` is blocked there and users should use the gateway-managed route without `--local`.
Updated `docs/deployment/deploy-to-remote-gpu.mdx` and `docs/monitoring/monitor-sandbox-activity.mdx` to remove `--local` from the affected examples.
Ran `npm run docs:strict` successfully with 0 errors and 2 Fern upgrade warnings.
Privately validated the fixed command on the provided NemoClaw test machine by running the no-`--local` command inside the `my-assistant` sandbox and receiving a normal response with `EXIT=0`.
Created signed branch commit `cd8e2f028616740cd8b87ce1648302a32ac02b0e` on branch `docs/remove-local-agent-flag` with message `doc: remove local flag from sandbox inference examples`.
Verified the commit with `git verify-commit HEAD` using the configured SSH signing key and DCO sign-off identity `Glenn-Agent <glenn_agent@163.com>`.
Pushed the branch to `glenn-agent/NemoClaw` over SSH after explicit user approval, then force-with-lease updated it after correcting the DCO email typo from `.con` to `.com`.
Created PR `NVIDIA/NemoClaw#3892` using `GH_TOKEN` loaded from the local secret file after confirming the token was present without printing it.
Initial PR checks were pending: CodeRabbit, assign-linked-issue-author, check-pr-limit, and onboard-entrypoint-budget.
