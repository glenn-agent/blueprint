# 2026-05-25

## Work

- [nemoclaw] Followed up on PR `NVIDIA/NemoClaw#4118` by pushing signed/DCO commit `adc9be8bc` (`fix: propagate debug sandbox probe failures`) from local branch `fix/debug-unknown-sandbox` to the `glenn-agent/NemoClaw` PR branch. Initial post-push checks showed public checks green and CodeRabbit status success, though the earlier review comment remains in the historical review thread.
- [nemoclaw] Selected issue `NVIDIA/NemoClaw#3681` after confirming no existing open PR mentioned it. Replaced the docs-only source-tree `scripts/backup-workspace.sh` guidance in `docs/manage-sandboxes/backup-restore.mdx` with host-installed `nemoclaw backup-all` guidance and noted that the script is source-tree/engineering-only. Opened PR `NVIDIA/NemoClaw#4164`: `https://github.com/NVIDIA/NemoClaw/pull/4164`.
- [nemoclaw] Selected issue `NVIDIA/NemoClaw#3432` only for the still-stale prerequisites wording after verifying current `docs/reference/architecture.mdx` already documents the Docker-driver topology and old PR `#3433` was closed unmerged. Updated `docs/get-started/prerequisites.mdx` to remove stale top-level k3s RAM wording and reframe the Docker storage note around the OpenShell gateway image. Opened PR `NVIDIA/NemoClaw#4165`: `https://github.com/NVIDIA/NemoClaw/pull/4165`.

## Checks

- [nemoclaw #4164] `npm run docs:strict` passed with 0 errors and 2 existing Fern upgrade warnings; `git diff --check` passed; signed/DCO commit `d800e7da7` verified with `git verify-commit HEAD`; initial public GitHub checks (`assign-linked-issue-author`, `check-pr-limit`, `onboard-entrypoint-budget`) passed; CodeRabbit was still pending at last check.
- [nemoclaw #4165] `npm run docs:strict` passed with 0 errors and 2 existing Fern upgrade warnings; `git diff --check` passed; signed/DCO commit `84ae0def4` verified with `git verify-commit HEAD`; initial public GitHub checks (`assign-linked-issue-author`, `check-pr-limit`, `onboard-entrypoint-budget`) passed; CodeRabbit was still pending at last check.

## Notes

- NVIDIA runner execution for new PRs is still gated by the repository's external validation flow (`copy-pr-bot`).
