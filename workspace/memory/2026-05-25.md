# 2026-05-25

## Work

- [nemoclaw] Followed up on PR `NVIDIA/NemoClaw#4118` by pushing signed/DCO commit `adc9be8bc` (`fix: propagate debug sandbox probe failures`) from local branch `fix/debug-unknown-sandbox` to the `glenn-agent/NemoClaw` PR branch. Initial post-push checks showed public checks green and CodeRabbit status success, though the earlier review comment remains in the historical review thread.
- [nemoclaw] Selected issue `NVIDIA/NemoClaw#3681` after confirming no existing open PR mentioned it. Replaced the docs-only source-tree `scripts/backup-workspace.sh` guidance in `docs/manage-sandboxes/backup-restore.mdx` with host-installed `nemoclaw backup-all` guidance and noted that the script is source-tree/engineering-only. Opened PR `NVIDIA/NemoClaw#4164`: `https://github.com/NVIDIA/NemoClaw/pull/4164`. Addressed CodeRabbit follow-up with signed/DCO commit `585265999` (`docs: align backup snapshot commands`) to use current `nemoclaw sandbox snapshot ...` syntax and link the source-tree helper directly.
- [nemoclaw] Selected issue `NVIDIA/NemoClaw#3432` only for the still-stale prerequisites wording after verifying current `docs/reference/architecture.mdx` already documents the Docker-driver topology and old PR `#3433` was closed unmerged. Updated `docs/get-started/prerequisites.mdx` to remove stale top-level k3s RAM wording and reframe the Docker storage note around the OpenShell gateway image. Opened PR `NVIDIA/NemoClaw#4165`: `https://github.com/NVIDIA/NemoClaw/pull/4165`. Addressed CodeRabbit follow-up with signed/DCO commit `57937702d` (`docs: align Docker-driver memory guidance`) to split one-sentence-per-line source formatting and align `docs/reference/troubleshooting.mdx` with the same Docker-driver memory guidance.

## Checks

- [nemoclaw #4164] `npm run docs:strict` passed with 0 errors and 2 existing Fern upgrade warnings before opening and after CodeRabbit follow-up; `git diff --check` passed; signed/DCO commits `d800e7da7` and `585265999` verified with `git verify-commit HEAD`; public GitHub checks (`assign-linked-issue-author`, `check-pr-limit`, `onboard-entrypoint-budget`) passed before follow-up and were rerunning after the follow-up push.
- [nemoclaw #4165] `npm run docs:strict` passed with 0 errors and 2 existing Fern upgrade warnings before opening and after CodeRabbit follow-up; `git diff --check` passed; signed/DCO commits `84ae0def4` and `57937702d` verified with `git verify-commit HEAD`; CodeRabbit passed after follow-up and the remaining public GitHub checks were rerunning/passing after the follow-up push.

## Notes

- NVIDIA runner execution for new PRs is still gated by the repository's external validation flow (`copy-pr-bot`).
