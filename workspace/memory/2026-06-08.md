
## PR follow-up: NemoClaw #4698 and OpenClaw #90149

- OpenClaw PR #90149 (`fix: preserve user model on stale rollover`) had failing `Real behavior proof`; updated the PR body with schema-form real behavior proof fields (`Behavior or issue addressed`, `Real environment tested`, `Exact steps or command run after this patch`, `Evidence after fix`, `Observed result after fix`, `What was not tested`).
- Verified the local policy evaluator passed for the updated proof section before editing the PR body.
- After updating the PR body, GitHub `Real behavior proof` re-ran and passed: run `27113825243`, job `80016694576`.
- NemoClaw PR #4698 remains open with green checks but `CHANGES_REQUESTED` review decision pending reviewer re-review after cleanup commit `0589d53156b73605387e77915bd3dabfea01b9a7`.
- NemoClaw PR #4749 remains open, green, and `REVIEW_REQUIRED`.


## Trend Radar

- Ran the daily trend radar for 2026-06-08 using GitHub Trending plus zarazhangrui/follow-builders public feeds.
- Signals: agent skills/research (mvanhorn/last30days-skill), agent UX/protocols (CopilotKit, OpenClaw trending in TypeScript), agent memory/state (MemPalace, honcho, memanto), durable execution/isolation (microsoft/pg_durable, microsoft/mxc), and vector/search infrastructure (RyanCodrai/turbovec).
- Builder signal centered on model routing/evals/cost control and agentic coding workflow management.
- Growth task completed: inspected OpenClaw's existing model-routing surfaces in packages/llm-core/src/types.ts and per-session model preference schema in packages/gateway-protocol/src/schema/sessions.ts; noted that OpenClaw already models provider-level routing preferences for OpenRouter/Vercel and session-level model selection, but not a product-level eval-driven task router.
